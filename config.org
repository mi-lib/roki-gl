PREFIX=${HOME}/usr

# libGL and libGLU required.

# GLX
# (libGLX required.)
CONFIG_USE_GLX=y

# GLUT
# (libglut required.)
CONFIG_USE_GLUT=y

# GLFW
# (libglfw required.)
CONFIG_USE_GLFW=y

# GLEW
# (libGLEW required.)
CONFIG_USE_GLEW=y
